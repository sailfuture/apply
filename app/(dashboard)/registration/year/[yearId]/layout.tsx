/**
 * Re-export of the apply-flow layout so /registration/year/[yearId] gets the
 * same chrome (sidenav suppression on the overview, save bar, modal, etc.).
 */
export { default } from "@/app/(dashboard)/apply/year/[yearId]/layout";
