/**
 * Re-application Student Details page.
 *
 * Re-export of the apply-flow students page. The page is flow-aware and
 * writes to the reapply progress row's `isStudentDetails` bool when
 * mounted under /reapply.
 */
export { default } from "@/app/(dashboard)/apply/year/[yearId]/students/page";
